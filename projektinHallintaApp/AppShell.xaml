<?xml version="1.0" encoding="utf-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:views="clr-namespace:projektinHallintaApp.Views"
       x:Class="projektinHallintaApp.AppShell">

    <!-- Kirjautumissivu (LoginView) -->
    <ShellContent Title="Login" Route="LoginView" ContentTemplate="{DataTemplate views:LoginView}" />
    <ShellContent Title="Main Page" Route="MainPage" ContentTemplate="{DataTemplate views:MainPage}" />
    <ShellContent Title="Create Project" Route="CreateProjectPage" ContentTemplate="{DataTemplate views:CreateProjectPage}" />

</Shell>
